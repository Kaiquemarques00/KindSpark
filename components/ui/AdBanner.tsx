/**
 * Default export for tooling (ESLint/TS). Metro resolves `AdBanner.native` / `AdBanner.web` at runtime.
 */
export type { AdBannerProps } from '@/components/ui/AdBanner.web';
export { AdBanner } from '@/components/ui/AdBanner.web';
